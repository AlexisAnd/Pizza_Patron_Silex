{% extends 'layout.html.twig' %}
{% block main_body %}
<div class=containeradminadd>
   <h1 class=passwordtitle>Cartes des desserts</h1>
    <table>
        <thead>
        <tr>
            <th>Nom</th>
            <th>Prix</th>
        </tr>
        </thead>
        <tbody>
        {% for dessert in desserts %}
            <tr>
                <td>{{ dessert.Name }}</td>
                <td>{{ dessert.SalePrice }} euros</td>
                <td><a href="{{ path('modifydesserts',{DessertId:dessert.DessertId}) }}" ><i class="fa fa-pencil" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <h3><a href="{{ path('menu') }}">Visualiser les changements</a></h3>
</div>
{%  endblock %}