{% extends 'layout.html.twig' %}
{% block main_body %}

 <div class="containeradminadd">
<h1 class="passwordtitle">Supprimer un panini</h1>
        <form action="" method="post" id="form">
            <fieldset>
                <label for="Name">Id</label>
                <input type="text" id="name" name="PaninisId" data-name="nom" data-required>
                <button type="submit" id="button">Valider</button>
                </fieldset>
    </form>
        <table>
            <thead>
            <tr>
                <th>Paninis</th>
                <th>Id</th>
            </tr>
            </thead>
            {% for panini in paninis %}
                <tr>
                    <td>{{ panini.Name }}</td>
                    <td>{{ panini.PaninisId }}</td>
                </tr>
            {% endfor %}
        </table>
       <h3><a href="{{ path('menu') }}">Visualiser les changements</a></h3>
    </div>

{% endblock %}